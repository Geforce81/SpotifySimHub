<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="SpotifyButtonBase"
           TargetType="{x:Type Button}">
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="MinHeight" Value="38" />
        <Setter Property="Padding" Value="16,9" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="ButtonBorder"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="Opacity"
                                    Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="Opacity"
                                    Value="0.72" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="BorderBrush"
                                    Value="#DFFFF0" />
                            <Setter TargetName="ButtonBorder"
                                    Property="BorderThickness"
                                    Value="2" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Cursor" Value="Arrow" />
                            <Setter TargetName="ButtonBorder"
                                    Property="Opacity"
                                    Value="0.42" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SpotifyPrimaryButton"
           BasedOn="{StaticResource SpotifyButtonBase}"
           TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#7FE8A8" />
        <Setter Property="BorderBrush" Value="#7FE8A8" />
        <Setter Property="Foreground" Value="#000000" />
    </Style>

    <Style x:Key="SpotifySecondaryButton"
           BasedOn="{StaticResource SpotifyButtonBase}"
           TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#1D3B29" />
        <Setter Property="BorderBrush" Value="#58B87B" />
        <Setter Property="Foreground" Value="#F2FFF7" />
    </Style>

    <Style BasedOn="{StaticResource SpotifySecondaryButton}"
           TargetType="{x:Type Button}" />
</ResourceDictionary>
